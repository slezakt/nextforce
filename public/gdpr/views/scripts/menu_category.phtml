<? if(!empty($this->menu['categories_menu'])){ ?>
    <? foreach($this->menu['categories_menu'] as $item){?>
        <? if(!empty($item['url'])){ ?>
        <li>
                <a href="<?=$item['url']?>"><?=$item['name']?></a>
        </li>
        <? } ?>
    <? } ?>
<? } ?>
